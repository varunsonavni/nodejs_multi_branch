pipeline {
    agent { label 'linux' }

    stages {
        stage('Hello') {
            steps {
                
                sh 'ls'
                sh 'pwd'
               

        
            }
        }
    }
}
